module.exports = function (app) {
  // This file is used to configure the development server middleware
  // The setupProxy.js approach uses the newer setupMiddlewares API automatically
  // No custom middleware configuration needed in this case
};
